@model IEnumerable<DataLayer.Models.BrokerDetail>

@{
    ViewData["Title"] = "RequestToBroker";
}

<h1>RequestToBroker</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Broker)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CustomerCount)
            </th>
            @*
            <th>
                @Html.DisplayNameFor(model => model.Commission)
            </th>
             *@
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BrokerId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CustomerCount)
            </td>
            @*
            <td>
                @Html.DisplayFor(modelItem => item.Commission)
            </td>
            
            <td>
                @Html.DisplayFor(modelItem => item.Broker.UserId)
            </td>
             *@
        <td>
            <a asp-action="AddRequest1" asp-route-BrokerId="@item.BrokerId ">Request</a>
            @*|
        <a asp-action="Details" asp-route-id="@item.BrokerId">Details</a> |
        <a asp-action="Delete" asp-route-id="@item.BrokerId">Delete</a>
            *@
        </td>
        </tr>
}
    </tbody>
</table>
