@model IEnumerable<DataLayer.Models.PolicyDetail>

@{
    ViewData["Title"] = "Insurer | Requests";
    Layout = "~/Views/Shared/_LayoutInsurer.cshtml";
}
<style>
    .hover:hover {
        box-shadow: 0 0 20px black;
        background-image: linear-gradient(to right,#000428 , #004e92);
        color:white;
        transition: all ease-in-out 0.2s;
    }
    .hover{
        box-shadow:2px 2px 8px black;
    }
    .ass {
        width: 150px;
    }

        .ass:hover {
            border-radius: 20px;

            @*transition:all ease-in-out 0.3s;
            *@
        }
</style>
<h3 class="text-center container" style="background-color: teal; padding: 10px; margin-top: 20px; box-shadow: 0px 15px 10px -15px #111;">Current Requests</h3>

<div class="container">
    @foreach (var item in Model)
    {
        <div class="card hover">
            <div class="card-body">
                <h4 class="card-title">@Html.DisplayNameFor(model => model.Asset): &nbsp;  @Html.DisplayFor(modelItem => item.Asset.AssetName)</h4>
                <h4> @Html.DisplayNameFor(model => model.Broker): &nbsp;  @Html.DisplayFor(modelItem => item.Broker.BrokerId)</h4>
                <h4>Policy Details:</h4>
                <div class="d-flex justify-content-between">
                    <div>
                        <p><strong>@Html.DisplayNameFor(model => model.StartDate):</strong> @Html.DisplayFor(modelItem => item.StartDate)</p>
                        <p><strong>@Html.DisplayNameFor(model => model.Duration):</strong> @Html.DisplayFor(modelItem => item.Duration)</p>
                        <p><strong>@Html.DisplayNameFor(model => model.MaturityAmount):</strong> @Html.DisplayFor(modelItem => item.MaturityAmount)</p>
                    </div>
                    <div>
                        <p><strong>@Html.DisplayNameFor(model => model.LumpSum):</strong> @Html.DisplayFor(modelItem => item.LumpSum)</p>
                        <p><strong>@Html.DisplayNameFor(model => model.Premium):</strong> @Html.DisplayFor(modelItem => item.Premium)</p>
                        <p><strong>@Html.DisplayNameFor(model => model.PremiumInterval):</strong> @Html.DisplayFor(modelItem => item.PremiumInterval)</p>
                    </div>
                    <div>
                        <p><strong>@Html.DisplayNameFor(model => model.PolicyStatus):</strong> @Html.DisplayFor(modelItem => item.PolicyStatus)</p>
                        <p><strong>@Html.DisplayNameFor(model => model.ReviewStatus):</strong> @Html.DisplayFor(modelItem => item.ReviewStatus)</p>
                        <p><strong>@Html.DisplayNameFor(model => model.Feedback):</strong> @Html.DisplayFor(modelItem => item.Feedback)</p>
                    </div>
                </div>
                <a class="btn btn-outline-success card-link ass" onclick="myMethod()" asp-action="Reject" asp-route-id="@item.PolicyId" asp-route-method="accepted">Accept</a>
                <a class="btn btn-outline-danger card-link ass" onclick="myMethod()" asp-action="Reject" asp-route-id="@item.PolicyId" asp-route-method="rejected">Reject</a>
            </div>
        </div>
        <br />
    }
</div>
